<x-site-layout title="Welcome Class">
{{--    <h2>Latest cities (out of {{$articles_count}})</h2>--}}
{{--    @foreach($cities as $article)--}}
{{--        <h2 class="font-bold">{{ $article->title }}</h2>--}}
{{--        <div>{{ $article->body }}</div>--}}
{{--    @endforeach--}}

</x-site-layout>
